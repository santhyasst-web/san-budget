import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // next-pwa will be configured when needed
  // For now, the manifest.json and apple-touch-icon cover basic PWA installability
};

export default nextConfig;
